# Python packages this module uses beyond the Odoo core stack.
# Only OFX import needs an extra package; CSV, QIF, CAMT.053 and MT940
# parse with the standard library. Deployments that read a repo-level
# requirements.txt (for example Odoo.sh) install this automatically;
# elsewhere run: pip install ofxparse
ofxparse
