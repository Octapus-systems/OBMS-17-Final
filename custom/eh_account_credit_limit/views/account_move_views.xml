<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_move_form_eh_credit" model="ir.ui.view">
        <field name="name">account.move.form.eh_credit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <div class="alert alert-warning" role="alert"
                     invisible="not eh_credit_warning">
                    <field name="eh_credit_warning" readonly="1" nolabel="1"/>
                </div>
            </xpath>
            <xpath expr="//field[@name='narration']" position="before">
                <group invisible="move_type not in ('out_invoice', 'out_refund') or state != 'draft'">
                    <field name="eh_credit_override_reason"
                           string="Credit override reason"
                           placeholder="Required only when posting would exceed the customer's credit limit"/>
                </group>
                <group invisible="not eh_credit_override_log_id">
                    <field name="eh_credit_override_log_id" readonly="1"
                           string="Credit override audit row"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
